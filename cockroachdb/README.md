### 起動方法

```bash
kubectl apply -f service.yaml
```

```bash
kubectl apply -f statefulset.yaml
```

```bash
kubectl get pods -w
```

```bash
kubectl apply -f cluster-init.yaml
```

```bash
kubectl get pods
```